 public static string timeConversion(string s)
    {
        DateTime hourNewFormat;
        hourNewFormat = Convert.ToDateTime(s);
        s = hourNewFormat.ToString("HH:mm:ss");
        
        return s;
    }
